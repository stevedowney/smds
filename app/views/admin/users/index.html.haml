%h1 Users

=paginate @users

%table.table.table-striped
  %thead
    %tr
      %th Admin?
      %th Username
      %th Email
      %th Created
      %th Confirmed
  %tbody
    - @users.each do |user|
      = render user

= paginate @users
